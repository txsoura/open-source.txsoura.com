name: CI

on:
  pull_request:
    branches: ['master']

  push:
    branches: ['master']

jobs:
  code-analysis:
    name: Code Analysis
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - uses: pnpm/action-setup@v3
        with:
          version: latest
          run_install: false
      - run: pnpm install
      - run: pnpm build
